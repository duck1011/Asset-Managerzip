Act as a Senior UI/UX Developer. We are completely redesigning the Hero section to match a vibrant, fluid gradient aesthetic with a central glassmorphism frame.

INSPIRATION: Fluid, overlapping colorful curves (blues, purples, oranges) on a dark base, with a glowing glass container on top.

TECH STACK: React + Vite, Tailwind CSS, Framer Motion.

EXECUTE THE FOLLOWING HERO REDESIGN (app/pages/Hero.jsx or equivalent):

1. The Fluid Gradient Background (The Base)

Main Hero Container: relative min-h-screen flex items-center justify-center overflow-hidden bg-slate-950
Create 3 large, absolute-positioned <motion.div> elements to act as the fluid color blobs. They must have extreme blur (blur-[150px]).
Blob 1 (Electric Cyan): absolute top-[-10%] left-[-10%] w-[600px] h-[600px] bg-cyan-500/60 rounded-full
Blob 2 (Deep Purple): absolute bottom-[-10%] right-[-10%] w-[700px] h-[700px] bg-purple-600/50 rounded-full
Blob 3 (Vibrant Orange): absolute top-[40%] left-[30%] w-[400px] h-[400px] bg-orange-500/40 rounded-full
Framer Motion Animation: Make these 3 blobs slowly drift and scale infinitely using Framer Motion (e.g., animate x, y, scale over 20-30 seconds with repeat: Infinity). This makes the background feel alive and fluid like the reference image.
2. The Glassmorphism Frame (The Container)

Directly centered over the blobs, create the glass frame: relative z-10 w-full max-w-3xl mx-6 rounded-[2rem] p-12 md:p-16 text-center
The Glass Effect: Apply the core glassmorphism classes: bg-white/5 backdrop-blur-2xl border border-white/20 shadow-2xl
The "Colors & Patterns" Inside the Frame (Crucial Step): To make the frame reflect the beautiful background inside itself, add an internal glowing gradient div before the text:
<div className="absolute inset-0 rounded-[2rem] overflow-hidden z-0">
Inside this, place a small, heavily blurred duplicate of the colors: <div className="absolute top-0 right-0 w-48 h-48 bg-cyan-500 blur-[80px] opacity-30"></div> and <div className="absolute bottom-0 left-0 w-48 h-48 bg-purple-600 blur-[80px] opacity-30"></div>
This makes the glass look like it's made of colorful, refracted light.
3. The Content (Inside the Frame)

Wrap the text and buttons in a relative z-10 div so they sit above the internal glass glows.
Title (Space Grotesk):
<h1 className="text-5xl md:text-7xl font-bold tracking-tight text-white leading-tight font-space-grotesk mb-6">  Think. Plan. Track.<br />  <span className="bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent">All in one.</span></h1>
(Notice the gradient text on "All in one" to tie in the colorful background).

Subtitle: <p className="text-lg text-slate-300 mb-10 max-w-lg mx-auto">Give your business the ultimate elevation. Manage consultations, bookings, and media seamlessly.</p>
Buttons:
Primary Button: bg-cyan-500 text-slate-900 font-semibold px-8 py-3.5 rounded-full hover:bg-cyan-400 transition-all shadow-lg shadow-cyan-500/25
Secondary Button: border border-white/20 text-white px-8 py-3.5 rounded-full hover:bg-white/10 transition-all ml-4
4. Global Polish:

Ensure the Navbar (components/Navbar.jsx) is using the dark glassmorphism style (bg-slate-950/40 backdrop-blur-xl border-b border-white/10) so it blends seamlessly into this new vibrant, dark hero background.
Make sure the blobs are large enough and blurred enough that they look like soft, sweeping curves of color (like the reference image), not sharp circles.